name: stylelinter

on: pull_request

permissions:
  actions: read
  checks: read
  contents: read
  deployments: none
  issues: read
  packages: none
  pull-requests: read
  repository-projects: none
  security-events: none
  statuses: read

jobs:
  phpcs:
    name: stylelinter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          lfs: false
          persist-credentials: false
      - name: stylelint
        uses: actions-hub/stylelint@1bbab914fb5f91d6231c1d92223990d4e5508495