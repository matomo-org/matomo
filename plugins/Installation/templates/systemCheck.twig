{% extends '@Installation/layout.twig' %}

{% block content %}

<div
    vue-entry="Installation.SystemCheck"
    show-next-step="{{ showNextStep|default(null)|json_encode|e('html_attr') }}"
    system-check-legend-url="{{ url|json_encode|e('html_attr') }}"
    error-type="{{ constant('Piwik\\Plugins\\Diagnostics\\Diagnostic\\DiagnosticResult::STATUS_ERROR')|json_encode|e('html_attr') }}"
    warning-type="{{ constant('Piwik\\Plugins\\Diagnostics\\Diagnostic\\DiagnosticResult::STATUS_WARNING')|json_encode|e('html_attr') }}"
    informational-type="{{ constant('Piwik\\Plugins\\Diagnostics\\Diagnostic\\DiagnosticResult::STATUS_INFORMATIONAL')|json_encode|e('html_attr') }}"
    system-check-info="{{ systemCheckInfo|json_encode|e('html_attr') }}"
    mandatory-results="{{ diagnosticReport.getMandatoryDiagnosticResults()|json_encode|e('html_attr') }}"
    optional-results="{{ diagnosticReport.getOptionalDiagnosticResults()|json_encode|e('html_attr') }}"
    informational-results="{{ diagnosticReport.getInformationalResults()|json_encode|e('html_attr') }}"
    is-installation="{{ isInstallation|default(null)|json_encode|e('html_attr') }}"
></div>

{% endblock %}
