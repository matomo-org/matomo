<div
  vue-entry="Marketplace.LicenseKey"
  is-valid-consumer="{{ isValidConsumer|default(null)|json_encode|e('html_attr') }}"
  is-super-user="{{ isSuperUser|default(null)|json_encode|e('html_attr') }}"
  is-auto-update-possible="{{ isAutoUpdatePossible|default(null)|json_encode|e('html_attr') }}"
  is-plugins-admin-enabled="{{ isPluginsAdminEnabled|default(null)|json_encode|e('html_attr') }}"
  paid-plugins-to-install-at-once="{{ paidPluginsToInstallAtOnce|default(null)|json_encode|e('html_attr') }}"
  install-nonce="{{ installNonce|default(null)|json_encode|e('html_attr') }}"
></div>
