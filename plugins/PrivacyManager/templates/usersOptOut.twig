{% extends 'admin.twig' %}

{% set title %}{{ 'PrivacyManager_GDPR'|translate }}{% endset %}

{% block content %}
    <div
        vue-entry="PrivacyManager.UsersOptOut"
        piwikurl="{{ piwikUrl|json_encode|e('html_attr') }}"
        language="{{ language|json_encode|e('html_attr') }}"
        is-super-user="{{ isSuperUser|json_encode|e('html_attr') }}"
        dnt-support="{{ dntSupport|json_encode|e('html_attr') }}"
        do-not-track-options="{{ doNotTrackOptions|default(null)|json_encode|e('html_attr') }}"
    ></div>
{% endblock %}
