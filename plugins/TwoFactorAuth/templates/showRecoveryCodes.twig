{% extends 'admin.twig' %}

{% block content %}
    <div
        vue-entry="TwoFactorAuth.ShowRecoveryCodesPage"
        codes="{{ codes|default(null)|json_encode|e('html_attr') }}"
        regenerate-success="{{ regenerateSuccess|default(null)|json_encode|e('html_attr') }}"
        regenerate-error="{{ regenerateError|default(null)|json_encode|e('html_attr') }}"
        regenerate-nonce="{{ regenerateNonce|json_encode|e('html_attr') }}"
    ></div>
{% endblock %}