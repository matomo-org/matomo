{% extends 'admin.twig' %}

{% set title %}{{ 'General_Security'|translate }}{% endset %}

{% block content %}

    <div
        vue-entry="UsersManager.AddNewToken"
        no-description="{{ noDescription|json_encode|e('html_attr') }}"
        form-nonce="{{ nonce|json_encode|e('html_attr') }}"
    ></div>

{% endblock %}
